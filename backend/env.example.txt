# 复制本文件为 .env 即可使用
# powershell: copy env.example.txt .env

# 数据库连接（本地默认）
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/english_app

# 大模型 API（兼容 OpenAI 协议，可用 DeepSeek / 通义千问 / OpenAI）
# 不填会走 mock 数据：每日新闻用内置示例 + 默写批改用本地词级 diff
LLM_API_KEY=
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat

# 应用配置
APP_HOST=0.0.0.0
APP_PORT=8000